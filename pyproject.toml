[project]
name = "hopper-egress-monitor"
version = "0.1.0"
description = "Logging all outbound connections from a Grace Hopper server."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
